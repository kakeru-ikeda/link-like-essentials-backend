export interface Stats {
    smile: number | null;
    pure: number | null;
    cool: number | null;
    mental: number | null;
}

export interface Skill {
    name: string;
    ap: string | null;
    effect: string | null;
}

export interface Trait {
    name: string;
    effect: string | null;
}
